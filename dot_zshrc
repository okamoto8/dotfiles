##### rbenv #####
export PATH="$HOME/.rbenv/bin:$PATH"
eval "$(rbenv init - zsh)"

##### Homebrew（先に置くのが安全） #####
export PATH="/opt/homebrew/bin:/opt/homebrew/sbin:$PATH"

##### rbenv shims #####
export PATH="$HOME/.rbenv/shims:$PATH"

##### pyenv #####
export PYENV_ROOT="$HOME/.pyenv"
export PATH="$PYENV_ROOT/bin:$PATH"
eval "$(pyenv init -)"

##### PostgreSQL 17 #####
export PATH="/opt/homebrew/opt/postgresql@15/bin:$PATH"

##### local bin（Claude / cursor-agent 用） #####
export PATH="$HOME/.local/bin:$PATH"

##### git profile switch #####
function gitmain() {
  git config --global user.name "okamoto8"
  git config --global user.email "ddi_00027@dk.daihatsu.co.jp"
}

function gitsub() {
  git config --global user.name "Turks08"
  git config --global user.email "turks7myhero@gmail.com"
}

##### Flutter #####
export PATH="$HOME/flutter/bin:$PATH"

##### oh-my-posh（必ず最後） #####
eval "$(oh-my-posh init zsh --config ~/.poshthemes/paradox.omp.json)"